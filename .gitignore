node_modules
.next
next-env.d.ts
tsconfig.tsbuildinfo

# Generated by scripts/make-live-stream.sh. Deliberately outside public/ — Next
# indexes that at build time and 404s files created later. Note also that MPEG-TS
# segments share the `.ts` extension with TypeScript, which is why tsconfig
# excludes this directory.
/.stream/
/.artifacts/
